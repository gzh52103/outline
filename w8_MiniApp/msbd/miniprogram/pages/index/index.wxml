<!--index.wxml-->
<view class="container">
  <mp-cells ext-class="my-cells" title="最新面试题">
      <mp-cell wx:for="{{newlist}}" value="{{item.question}}" footer="{{item.answer + '回答'}}"></mp-cell>
      <!-- <mp-cell>
          <view>标题文字（使用slot）</view>
          <view slot="footer">说明文字</view>
      </mp-cell>
      <mp-slideview buttons="{{slideButtons}}" bindbuttontap="slideButtonTap">
          <mp-cell value="左滑可以删除" footer="说明文字"></mp-cell>
      </mp-slideview> -->
  </mp-cells>
  <mp-cells ext-class="my-cells" title="热门面试题">
      <mp-cell wx:for="{{hotlist}}" value="{{item.question}}" footer="{{item.answer + '回答'}}" bind:tap="gotoDetail" data-iqid="{{item._id}}"></mp-cell>
  </mp-cells>

</view>
